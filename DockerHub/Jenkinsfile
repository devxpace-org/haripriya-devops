pipeline {
    agent any

    stages {
        stage('Build and Deploy') {
            steps {
                sh '''
                    sudo docker build -t haripriya/devxpace:nicejob-${version} .
                    sudo docker push haripriya/devxpace:nicejob-${version}
                   '''          
            }
         }
    }
}