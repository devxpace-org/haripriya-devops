pipeline {
    agent any

    stages {
        stage('Build and Deploy') {
            steps {
                sh '''
                    sudo docker build -t hrthummala/devxpace:supriya-${version} DockerHub/
                    sudo docker push hrthummala/devxpace:supriya-${version}
                   '''          
            }
         }
    }
}
