pipeline {
    agent any

    stages {
        stage('Build and Deploy') {
            steps {
                sh '''
                    sudo docker build -t hrthummala/devxpace:chethna-${version} DockerHub/
                    sudo docker push hrthummala/devxpace:chethna-${version}
                   '''          
            }
         }
    }
}
