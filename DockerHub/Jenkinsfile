pipeline {
    agent any

    stages {
        stage('Build and Deploy') {
            steps {
                sh '''
                    sudo docker build -t hrthummala/devxpace:haripriya-${version} .
                    sudo docker push hrthummala/devxpace:haripriya-${version}
                   '''          
            }
         }
    }
}
