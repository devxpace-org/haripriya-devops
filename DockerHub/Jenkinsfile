pipeline {
    agent any

    stages {
        stage('Build and Deploy') {
            steps {
                sh '''
                    docker build -t haripriya/devxpace:nicejob-${version} DockerHub/Dockerfile
                    docker push haripriya/devxpace:nicejob-${version}
                   '''          
            }
         }
    }
}
