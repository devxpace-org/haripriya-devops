pipeline {
    agent any

    stages {
        stage('Build and Deploy') {
            steps {
                sh '''
                    docker build -t haripriya/devxpace:nicejob-${version} DockerHub
                    docker push haripriya/devxpace:nicejob-${version}
                   '''          
            }
         }
    }
}
