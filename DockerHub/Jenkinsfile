pipeline {
    agent any

    stages {
        stage('Build and Deploy') {
            steps {
                sh '''
                    sudo docker build -t hrthummala/devxpace:sumanth-${version} DockerHub/
                    sudo docker push hrthummala/devxpace:sumanth-${version}
                   '''          
            }
         }
    }
}
