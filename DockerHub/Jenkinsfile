pipeline {
    agent any

    stages {
        stage('Build and Deploy') {
            steps {
                sh '''
                    sudo docker build -t hrthummala/devxpace:praneeth-${version} DockerHub/
                    sudo docker push hrthummala/devxpace:praneeth-${version}
                   '''          
            }
         }
    }
}
